#include "minimax.h"

namespace c4 {

  std::pair<size_t, double> MinimaxChooser::minimax(Board *board, size_t max_depth,
                                    bool maximize,
						    Board::Cell maximizing_color) {
    return std::make_pair<size_t, double>(0, 0.0);
  }


}  // namespace c4
